BETTER_AUTH_SECRET=local-dev-auth-secret-do-not-use-in-production
DATABASE_URL=postgresql://openar:openar_dev@localhost:5532/openar
BETTER_AUTH_URL=http://localhost:3001
BETTER_AUTH_BASE_PATH=/api/auth
# CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Production: root domain for cross-subdomain cookies (app/api/auth on different subdomains).
# Enables cookies to be sent to api.demo.bridgable.ai when user logs in via auth.
# BETTER_AUTH_COOKIE_DOMAIN=demo.bridgable.ai
