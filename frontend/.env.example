# Backend API URL (WebSocket URL is derived automatically)
VITE_API_URL=http://localhost:8000

# Better Auth frontend client
VITE_BETTER_AUTH_URL=http://localhost:3001

# Optional: Override WebSocket URL (only if different from API URL)
# VITE_WS_URL=wss://api.demo.bridgable.ai

# Optional: Better Auth API base path (defaults to /api/auth)
# VITE_BETTER_AUTH_BASE_PATH=/api/auth
