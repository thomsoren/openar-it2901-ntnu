DATABASE_URL=postgresql+psycopg://openar:openar_dev@localhost:5532/openar
JWT_SECRET_KEY=local-dev-jwt-secret-do-not-use-in-production
BETTER_AUTH_BASE_URL=http://localhost:3001
BETTER_AUTH_BASE_PATH=/api/auth
CORS_ALLOW_PRIVATE_NETWORK_ORIGINS=false

# Production example:
# ENV=production
# NODE_ENV=production
# BETTER_AUTH_BASE_URL=https://ar.bridgable.ai

# Optional — features degrade gracefully without these
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_PUBLIC_BASE_URL=
# AIS_CLIENT_ID=
# AIS_CLIENT_SECRET=

# IDUN Remote Inference (HPC cluster)
# Set IDUN_ENABLED=true to offload inference to IDUN via WebSocket.
# IDUN workers connect outbound to the /api/idun/ws endpoint.
# IDUN_ENABLED=false
# IDUN_API_KEY=generate-a-strong-random-key
# IDUN_FRAME_JPEG_QUALITY=80
# IDUN_TARGET_SEND_FPS=15
